// import { run, newSignal } from "@fullkit/client";

// run(() => {
//   const [count, setCount] = newSignal(0);
//   return {
//     count,
//     setCount,
//   };
// }, document.getElementById("app")!);
